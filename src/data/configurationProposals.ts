import type { ConfigurationProposal } from "@/types/configuration";

// No backend contract is available yet; render the empty state instead of demo records.
export const configurationProposals: ConfigurationProposal[] = [];
