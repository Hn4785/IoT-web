# Checkpoint D — local release candidate

Date: 2026-09-20  
Status: Verified locally; not committed or pushed

## Evidence

- Reproducible Node 24/pnpm 11 image builds and runs as non-root `node`.
- Production image liveness, PostgreSQL readiness and runtime release contract
  pass against `iot_dev`.
- `iot_dev` was backed up and restored without overwrite into
  `iot_restore_d_20260920`; the same production image passed readiness against
  the restored database.
- Development/test OpenAPI contains the required Phase A–C frontend paths.
- CI defines immutable install, isolated migrations, static gates, full coverage,
  high/critical dependency audit, tracked-file secret scan and image smoke.
- Image smoke exposed and fixed one production-only defect: `dotenv` was pruned
  as a dev dependency even though `dist/main.js` imports it.
- Full backend verification passed: 58/58 test files and 369/369 tests; coverage
  is 88.48% statements, 77.60% branches, 93.24% functions and 90.29% lines.
  Format, typecheck, lint, build, migration status, secret scan and diff check
  also pass. Production audit has one tracked moderate Prisma/mysql2 advisory and
  no high/critical finding.

## Local acceptance

OP-3 and the local portions of OP-4/OP-5 are complete. The backend is ready for
frontend contract integration and tester rehearsal with controlled sample data.

## External release gates

Checkpoint D production acceptance remains open until staging, live hardware,
ingress/TLS/proxy, private metrics, backup ownership/RPO/RTO and MFA decisions
have named owners and evidence. No public metrics endpoint or deployment
topology was invented in this checkpoint.
