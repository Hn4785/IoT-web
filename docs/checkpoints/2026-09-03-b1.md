# Checkpoint B1 — Authorized station hierarchy

Date: 2026-09-03

Branch: `codex/integration-core`

State: local only; not pushed or merged

## Delivered scope

- Added strict, opaque and bounded cursors for farm, plot, station, client-station
  and future soil-history pagination.
- Added Bearer-authenticated farm, plot, station list and station detail routes.
- Admin reads the current registry; Farmer reads only farms covered by current
  membership; Client Developer Bearer access is denied.
- Missing and out-of-scope resources share `404 NOT_FOUND`; internal
  `upstreamCode` is exposed only as public `code`.
- Added a fail-closed, idempotent demo registry command for exact `/iot_dev`.

## Verification evidence

| Command                                                                                                | Result                                                       |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ |
| `pnpm test test/integration/station-data/hierarchy.spec.ts src/station-data/seed-station-demo.spec.ts` | PASS — 2 files, 11 tests                                     |
| `pnpm test test/integration/identity/scopes.spec.ts`                                                   | PASS — 1 file, 3 tests                                       |
| `pnpm typecheck`                                                                                       | PASS                                                         |
| `pnpm build`                                                                                           | PASS; Prisma Client generated and TypeScript build completed |
| `pnpm lint`                                                                                            | PASS — zero warnings/errors                                  |
| `pnpm test`                                                                                            | PASS — 30 files, 132 tests                                   |
| `pnpm format:check`                                                                                    | PASS                                                         |
| Seed CLI against `/iot_test` with confirmation                                                         | PASS — refused before database connection                    |

The hierarchy integration test uses real HTTP and PostgreSQL. It proves Admin
visibility, Farmer membership restriction, Farmer cross-farm denial as 404,
Client Developer browser denial as 403, all four browser routes, and public
station mapping without `upstreamCode`. It creates 105 deterministic pagination
farms in addition to the two authorization fixtures, verifies 50-item pages with
no duplicate IDs, and verifies a farm cursor cannot be reused on a plot route.

The seed unit suite proves refusal for production, `/iot_test`, another database,
missing confirmation and a wrong confirmation flag. It proves the accepted
`/iot_dev` boundary, two-run idempotence, exactly one Farm Demo, one Plot Demo,
NODE01/NODE02, and fail-closed behavior for duplicate/conflicting natural keys.
The test suite never invokes the CLI and never connects to `iot_dev`.

## Local commits

- `4eba05c feat: define station data contracts`
- `e0587ce feat: expose authorized station hierarchy`
- Task 3 seed/checkpoint commit follows this evidence document.

Checkpoint B1 is complete. Tasks 4–6 remain outside this checkpoint and were not
started.
