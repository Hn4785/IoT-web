# Checkpoint C1 — alert lifecycle gate

Status: complete (2026-09-16)

Implemented and verified:

- scoped Admin/Farmer alert-rule APIs; Client Developer denied;
- confirmed metadata, one enabled rule per station/field and atomic create idempotency;
- optimistic rule revision updates and disable-to-resolve transition;
- pure two-sample evaluator with persisted counters and observation timestamp deduplication;
- PostgreSQL evaluator lease, bounded batches and per-station failure isolation;
- scoped alert list/detail plus retry-safe acknowledge and manual resolution;
- manual resolution rejected until the latest accepted sample is normal;
- immutable lifecycle revisions and one unresolved alert per rule remain database-enforced.

Focused evidence is in `test/integration/alert-config/checkpoint-c1.spec.ts`,
`rules.spec.ts`, contract unit tests and the OpenAPI test. Notifications (AC-4)
start after this checkpoint. A deeper connected-path code review is intentionally
scheduled for the next work session.
